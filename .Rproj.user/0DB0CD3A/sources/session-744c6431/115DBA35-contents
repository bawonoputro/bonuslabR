#' Print method for ridgereg_qr objects
#'
#' Nicely prints a fitted `ridgereg_qr` model, showing the original call/formula
#' and the estimated coefficients from the QR-based ridge regression.
#'
#' This is the S3 `print()` method for objects of class `ridgereg_qr`.
#'
#' @param x A `ridgereg_qr` model object returned by [ridgereg_qr()].
#' @param ... Additional arguments (currently ignored). Included for S3
#'   method compatibility.
#'
#' @return Invisibly returns `x`, the input model object.
#'
#' @export
print.ridgereg_qr <- function(x, ...) {
  cat("Call:\n")
  print(x$formula)

  cat("\nCoefficients:\n")
  print(drop(x$coefficients))

  invisible(x)
}
